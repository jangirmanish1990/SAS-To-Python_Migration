# ─────────────────────────────────────────────
# SAS-to-Python Migration Assistant
# Requirements — Python 3.12
# Install: pip install -r requirements.txt
# ─────────────────────────────────────────────

# ── LLM + LangChain ──────────────────────────
openai==1.51.0
langchain==0.3.1
langchain-core==0.3.6
langchain-openai==0.2.2
langchain-community==0.3.2

# ── LangGraph (Phase 3 — multi-agent) ────────
langgraph==0.2.16

# ── RAG + Vector Store (Phase 2) ─────────────
chromadb==0.5.5
langchain-chroma==0.1.3
sentence-transformers==3.1.1
tiktoken==0.7.0

# ── Data ─────────────────────────────────────
pandas==2.2.2
pyarrow==17.0.0
python-dateutil==2.9.0

# ── PySpark (Phase 3 — pyspark target) ───────
# Uncomment when needed — requires Java 11+
# pyspark==3.5.3

# ── SAS file reading (optional) ──────────────
# Uncomment to read .sas7bdat files directly
# pyreadstat==1.2.7

# ── Config + Env ─────────────────────────────
python-dotenv==1.0.1
pydantic==2.8.2
pydantic-settings==2.4.0

# ── Testing ───────────────────────────────────
pytest==8.3.2
pytest-asyncio==0.24.0
pytest-cov==5.0.0

# ── Dev utilities ────────────────────────────
ruff==0.6.9
